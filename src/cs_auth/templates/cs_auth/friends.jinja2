{% extends "base.jinja2" %}

{% block style %}
    {{ super() }}
    <style>
    #friendship-block{
        padding: 10px;
    }
    .friend-block{
        padding: 5px;
        display: inline;
    }
    .friend-block p{
        display: inline;
        padding-left: 4px;
    }
    .friend-block img{
        height: 30px;
    }
    </style>
{% endblock %}

{% block content %}
    <div>
        <template is="dom-bind">
            <paper-tabs selected="{{ '{{ selected }}' }}" id="contact-tabs">
                <paper-tab>{{ _('Friends') }}</paper-tab>
                <paper-tab>{{ _('Colleagues') }}</paper-tab>
                <paper-tab>{{ _('Staff') }}</paper-tab>
                <paper-tab>{{ _('Pending') }}</paper-tab>
            </paper-tabs>
            <iron-pages selected="{{ '{{ selected }}' }}">
                <div>
                    <div id="friendship-block">
                    {% for friend in user.friends %}
                            <div class="friend-block">
                                <a href="/accounts/{{ friend.username }}"> 
                                {% if friend.profile %}
                                    <img class="friendiship-user-image" src="{{ friend.profile.get_mugshot_url }}" />
                                {% endif %}
                                <p> {{ friend.username  }} </p>
                                </a>
                            </div>
                    {% endfor %} 
                    </div>
                </div>
                <div>
                    {% for collegues in user.colleagues %}
                       <p> {{ colleagues.username  }} </p>
                    {% endfor %}
                </div>
            </iron-pages>

         </template>
    
    
    </div>
{% endblock %}
